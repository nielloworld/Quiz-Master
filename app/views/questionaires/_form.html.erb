<%= form_for(questionaire) do |f| %>
  <% if questionaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(questionaire.errors.count, "error") %> prohibited this questionaire from being saved:</h2>

      <ul>
      <% questionaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %>
    <%= f.text_field :question %>
  </div>

  <div class="field">
    <%= f.label :answer %>
    <%= f.text_field :answer %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
